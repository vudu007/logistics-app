# Flask Configuration
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://user:password@localhost/dbname

# Email Configuration (for snag notifications)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=noreply@logisticspro.com

# Google API Configuration
# Path to your Google service account credentials JSON file
GOOGLE_CREDENTIALS_FILE=google_credentials.json

# Note: To obtain Google API credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google Sheets API and Google Drive API
# 4. Create Service Account credentials
# 5. Download the JSON key file and save as google_credentials.json
# 6. Share your Google Sheet with the service account email
# 7. Share your Google Drive folder with the service account email
